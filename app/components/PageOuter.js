import React from "react";
import styled from "styled-components";
import { colors } from "../theme";

const PageOuter = styled.ScrollView`
  flex: 1;
  background: ${colors.primary};
  padding: 20px;
`;

export default PageOuter;
